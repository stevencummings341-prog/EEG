# Intended package set for the `mi_torch` conda env (python 3.10).
# Versions below are the ones VERIFIED in mi_torch at project setup.
#
# IMPORTANT: torch in mi_torch is currently a CPU-only build. For GPU training
# install a CUDA-matched wheel (cluster module is cuda/11.8), e.g.:
#   pip install --index-url https://download.pytorch.org/whl/cu118 torch torchvision torchaudio
# See docs/ENVIRONMENT.md. Do NOT pin a plain `torch==2.6.0` here for GPU use.

# --- core scientific stack ---
numpy==2.2.5
scipy==1.15.3
pandas==2.2.3
scikit-learn==1.7.1
matplotlib
tqdm
pyyaml
h5py==3.14.0

# --- EEG ---
mne==1.10.0

# --- deep learning ---
# torch / torchvision / torchaudio: install the cu118 build separately (see above).
einops==0.8.1

# --- optional (install when needed; not part of the minimal setup) ---
# braindecode        # reference baselines (EEGNet/DeepConvNet/FBCNet)
# pyriemann          # Riemannian expert (v2)
# moabb              # benchmarking helpers (v2)
# tensorboard        # experiment tracking
